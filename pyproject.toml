[project]
name = "traffic-light-prediciton"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Linus Pummer", email = "linus.pummer@web.de" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
traffic-light-prediciton = "traffic_light_prediciton:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
